{
  "name": "mylife",
  "version": "0.1.0",
  "private": true,
  "description": "MyLife — personal productivity & Islamic lifestyle app (Firebase migration, Phase 1 foundation).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^11.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
