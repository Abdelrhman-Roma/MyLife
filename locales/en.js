// English locale. Since the app's source strings ARE English, this file is
// intentionally small — t() already falls back to the original string when
// no entry exists. Kept mainly so the locale pair is explicit/extensible
// (e.g. for future US/UK spelling variants or pluralization tweaks).
window.MOMENTUM_LOCALES = window.MOMENTUM_LOCALES || {};
window.MOMENTUM_LOCALES.en = {
  // Intentionally sparse — English strings pass through unchanged.
};
